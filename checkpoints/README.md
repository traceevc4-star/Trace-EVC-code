# Checkpoints

Trained weights and the pretrained vocoder live here (none are committed).

```
bigvgan_v2_44khz_128band_512x/    BigVGAN-v2 vocoder — ONLY for the optional 44.1 kHz variant
                                  (the default 16 kHz path uses SpeechBrain HiFi-GAN, auto-fetched)
emo_compass/                      trained Emo-Compass flow + frozen heads
ease/                             trained EASE speaker encoder (ease.pt)
synthesis/                        trained DurFlex-EVC synthesis checkpoints
```
